function sum(prev, current) {
    return prev + current
}

function substract(prev, current) {
    return prev - current
}

function multiplay(prev, current) {
    return prev * current
}

function division(prev, current) {
    return prev / current
}